# A-047 — Gemini assignment (`gemini`) — BLOCKED: repository handoff missing

Recorded by Claude Code under A-044-R2 so that ownership of A-047 is machine-readable. **No completion is claimed.** No A-047 commit, file or brief exists on any branch of this repository; the output is reported to exist only outside GitHub.

To complete it, Gemini commits its output with a `[TOOL:GEMINI][TASK:A-047] …` completion commit, pushes, and replaces this record with a completed `HANDOFF.json` (see `docs/ai/README.md`). The registry keeps A-047 OPEN until then.

Machine-readable record: [HANDOFF.json](HANDOFF.json).
