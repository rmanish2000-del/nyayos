# A-042 handoff — Minor finding m-1 closure

> Backfilled by A-044 from the status registry, the assignment register and the A-042 commit, so that the AI operating system starts from a recorded state. A-042 itself predates `docs/ai/`.

**Assignment:** A-042
**Tool:** claude-code
**Status:** REVIEW
**Branch:** feature/fma-foundation-v1
**Baseline:** 13765b9b97be2dc06942711373c203f7f2b7ed42
**Commit:** fc740380eb6282d3b6a174b8a01a93e7528bddd8
**Pushed:** yes
**Deployment:** none

## Result

A-032 minor finding m-1 is closed. The finding was reproduced on migrations `0001`–`0007`: a dispute owner and a same-tenant editor could each set `disputes.status` directly. Migration `db/migrations/0008_dispute_status_authorization.sql` revokes `UPDATE (status)` from `nyayos_authenticated` (title and category-label edits kept) and makes status follow the deletion workflow only, through the trigger `deletion_requests_dispute_status`: a dispute-scope request sets `deletion_requested`, and an undo sets `active` when no other request is open, audited as `deletion.undone` in the same transaction. The purge worker never sets status.

## Evidence

- `db/tests/dispute_status_0001.sql` 21/21 (it fails on the baseline, which is the reproduction); negative controls (re-grant, missing undo audit) detected.
- Regression on fresh disposable containers: smoke 87/87, deletion purge 55/55, deletion scope 28/28, deletion authz 25/25, audit atomicity 21/21, hash vectors 16/16, audit concurrency matrix 5/5, purge race 3/3. Vitest 199/199; typecheck, lint (0 errors), build and schema-lint clean.
- PR #2 checks (task-gate, dependency-check, schema-lint) passed on `fc74038`; PR draft and unmerged; 0 deployments.

## Limitations

- Dispute status is driven only by dispute-scope requests; undo of document or account requests is not audited (pre-existing, outside m-1).
- A-032 minors m-2 to m-12 remain open as non-blocking follow-ups.
- Migrations have run only on disposable local containers; no environment exists.

## Next

Founder merge-readiness review of draft PR #2. Every fix A-032 listed as required before merge is closed.
